<!DOCTYPE html>
<html<%= config.language ? " lang=" + config.language.substring(0, 2) : ""%>>
<%- partial('_partial/head') %>
<body class="max-width mx-auto px3 <%- theme.direction -%>"<% if (is_post()) { %> data-copy-label="<%= __('tooltip.copy_tip') %>" data-copied-label="<%= __('tooltip.copied') %>"<% } %>>
    <% const layoutName = page && page.layout; %>
    <% const showChrome = layoutName !== 'profile'; %>
    <% if (is_post()) { %>
      <%- partial('_partial/post/actions_desktop') %>
    <% } %>
    <div class="content index py4 <%= is_home() ? 'h-card' : '' %>">
        <% if (!is_post() && showChrome) { %>
          <%- partial('_partial/header') %>
        <% } %>
        <%- body %>
        <% if (is_post()) { %>
          <%- partial('_partial/post/actions_mobile') %>
        <% } %>
        <% if (showChrome) { %>
          <%- partial('_partial/footer') %>
        <% } %>
    </div>
    <%- partial('_partial/styles') %>
    <%- partial('_partial/scripts') %>
</body>
</html>
